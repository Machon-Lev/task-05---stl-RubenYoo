I choosed to use a map container, because the container have the abillity to read a value from a key in O(1)
and in our case, when we need to search a location of a specific city, it is very useful.
futhermore, with this container we can easily add and remove a city in O(1).